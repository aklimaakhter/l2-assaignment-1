<!-- Any -->
In TypeScript, any is called a "type safety hole" because it disables TypeScript’s type checking.
When a variable is any, you can do anything with it, and TypeScript will not warn you — even if the code is wrong.But when run the code then crash.that's why unsafe for checking.

<!-- Unknown -->
unknown is the safer . because TypeScript forces you to check the type before using the value.

<!-- type Narrowing -->
type narrowing is works board to specify.
